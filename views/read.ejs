<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>

</head>
<body class="bg-zinc-900 min-h-screen">
    <div class="w-full h-full bg-zinc-900 p-10 text-white">
        <div class="nav mb-10">
            <a class="text-blue-500" href="/">Back To Home</a>
        </div>
        <h1 class="text-3xl font-bold mt-5 mb-5">All User</h1>

                <div class="users flex  gap-8  flex-wrap">
                    <% if(users.length > 0){ %>
                        <% users.forEach(function(user){ %>
                    
                            <div class="user w-65 p-4 bg-zinc-800 rounded-lg ">
                                <div class="w-full h-72 bg-zinc-700 overflow-hidden rounded-lg">
                                    <img src="<%= user.image %>" alt="" class="w-full h-full object-cover object-top rounded-lg">
                                </div>
                                <h3 class="text-xl mt-3 tracking-tighter "><%= user.name %></h3>
                                <h5 class="text-zinc-500"><%= user.email %></h5>
                                <div class="flex gap-3">
                                    <a href="/edit/<%= user._id %>" class="text-zinc-200 mt-10  p-2 rounded-lg">Edit</a>
                                    <a href="/delete/<%= user._id %>" class="text-red-500 mt-10 p-2 rounded-lg">Delete</a>
                                    
                                </div>
                            </div>
                            <% })%>
                    
                    <% }else{
                        %>
                        <h1 class="text-2xl">No User Found</h1>
                    <%
                       } %>
          

       
 

        </div>

        </div>
   
       
    </div>
</body>
</html>